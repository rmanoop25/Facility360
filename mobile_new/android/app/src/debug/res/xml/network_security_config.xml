<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow all cleartext HTTP for debug builds (local dev server on any IP) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
